services:
  temporal:
    build: .
    container_name: temporal
    restart: unless-stopped
    ports:
      - "3010:3000"
    networks:
      - caddy_network

networks:
  caddy_network:
    external: true
